{
  "name": "server",
  "description": "Deco MCP server built on top of Cloudflare Workers",
  "version": "1.0.0",
  "scripts": {
    "dev": "deco dev --clean-build-dir ./view-build",
    "deploy": "wrangler build && deco deploy ./dist",
    "gen": "deco gen --output=deco.gen.ts",
    "gen:self": "deco gen --self=$DECO_SELF_URL --output=deco.gen.ts",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": {
    "@deco/workers-runtime": "npm:@jsr/deco__workers-runtime@0.11.1",
    "jose": "^6.0.12",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "deco-cli": "^0.14.2",
    "drizzle-kit": "^0.31.4",
    "wrangler": "^4.28.0"
  }
}
